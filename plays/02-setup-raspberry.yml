---
- name: finish setting up
  hosts: rasbperries
  roles:
    - set-hostname
    - configure-static-ip
    - disable-ssh-password
    - remove-pi-user
    - set-hosts
    - activate-cgroup-support
    - enable-legacy-iptables
